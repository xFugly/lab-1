#include <iostream> // Подключаем библиотеку для ввода-вывода
#include <string>   // Подключаем библиотеку для работы со строками

using namespace std; // Используем стандартное пространство имен

int main() // Главная функция программы
{
    // === ЗАДАНИЕ 1 ===
    
    // 1. Выводим строку "First string" в консоль
    cout << "First string" << endl;
    
    // 2. Выводим две строки с использованием endl
    cout << "Первая строка" << endl;
    cout << "Вторая строка" << endl;
    
    // 3. Для решения проблемы с кодировкой в Windows
    #ifdef _WIN32
        system("chcp 65001"); // Устанавливаем кодировку UTF-8
    #endif
    
    // 4. Выводим строку со спецсимволами (экранирование)
    cout << "Спецсимволы \"\\\"" << endl;
    
    // === ЗАДАНИЕ 2 ===
    
    // 1. Создаем переменную типа int со значением 150
    int number1 = 150;
    
    // 2. Создаем переменную типа float со значением 15,933
    float number2 = 15.933f;
    
    // 3. Создаем переменную типа short для экономии памяти
    short number3 = 250;
    
    // 4. Выводим все переменные в консоль
    cout << "number1 = " << number1 << endl;
    cout << "number2 = " << number2 << endl;
    cout << "number3 = " << number3 << endl;
    
    // 5. Создаем три переменные для даты рождения
    int day = 24;
    string month = "мая";
    int year = 2006;
    
    // 6. Выводим дату рождения в нужном формате
    cout << "Моя дата рождения: " << day << " " << month << " " << year << " года" << endl;
    
    // 7. Создаем две константы
    const float PI_VALUE = 2.3f;
    const string OS_NAME = "WINDOWS";
    
    // 8. Выводим значения констант через пробел
    cout << PI_VALUE << " " << OS_NAME << endl;
    
    return 0; // Завершаем программу
} // Конец функции main